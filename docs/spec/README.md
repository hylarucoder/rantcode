# Specs

This folder is for detailed, document‑driven specs.

Suggested patterns:

- `*.spec.mdx` for feature specifications
- `*.task.mdx` or related MDX files that describe flows tied to specific tasks

Specs here are what the Explorer / Preview views in rantcode are primarily built around.
